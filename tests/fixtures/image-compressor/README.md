# 圖片壓縮測試素材

`transparent.webp` 由本機 Chromium 的 OffscreenCanvas 產生：80 × 40 像素，左半為純紅色、右半透明，以 WebP quality=1 編碼。沒有使用者圖片、第三方圖片或來源中繼資料。

PNG、帶 EXIF orientation 6 的 JPEG、12 MP 漸層雜訊圖、損毀與超大容器由測試即時合成。大圖種子固定為 42。測試僅在本機讀取／下載；網路邊界檢查涵蓋測試檔名與輸出檔名。
